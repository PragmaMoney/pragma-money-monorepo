{
  "id": "pragma-agent",
  "name": "pragma-agent",
  "version": "1.0.0",
  "description": "PragmaMoney tools for AI agents — register, pay, and call services on Monad Testnet",
  "skills": ["skills/pragma-money"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "relayerUrl": {
        "type": "string",
        "description": "Proxy relayer URL (default: http://localhost:4402)"
      },
      "bundlerUrl": {
        "type": "string",
        "description": "Bundler RPC URL for UserOp submission (e.g., Alchemy)"
      }
    }
  },
  "uiHints": {
    "bundlerUrl": { "label": "Bundler URL", "placeholder": "https://monad-testnet.g.alchemy.com/v2/<key>" },
    "relayerUrl": { "label": "Relayer URL", "placeholder": "http://localhost:4402" }
  }
}
